# --- API Configuration ---
# In development, leave blank to use the Vite proxy (/api)
# In production, set to your backend origin (e.g., https://api.drmirror.com); /api is added automatically
VITE_API_BASE_URL=

# --- Dev Proxy (Local only) ---
# Target backend for the Vite dev server proxy
VITE_API_PROXY_TARGET=http://localhost:5223
